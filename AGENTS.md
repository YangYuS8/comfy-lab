# AGENTS.md

本文件约束所有在本仓库工作的 Agent，包括 Luna、ChatGPT、Codex 及其子代理。

## 项目目标

把 Fedora 上的 ComfyUI 建设成可由 Agent 控制、验证、复现和审查的视觉生成执行环境。

## 工作方式

1. 每项任务使用独立分支，不直接修改 `main`。
2. 先检查当前仓库、ComfyUI 服务和已安装资源，再制定变更。
3. 优先调用 ComfyUI HTTP API；不要使用浏览器自动点击作为正式实现。
4. 工作流以 API Format JSON 作为自动化资产，以普通 Workflow JSON 或 PNG 元数据作为可视化来源。
5. 每次实验只改变已记录的变量，并留下运行报告。
6. 完成后创建 Pull Request，说明验证方法、结果和遗留问题。

## 必须提交的内容

新增一条可复现工作流时，至少提交：

- `workflows/<category>/<name>-api.json`
- `manifests/models.yaml` 中涉及的模型记录
- 调用或验证脚本
- `reports/<experiment>.md`

报告至少包含：

- ComfyUI、Python、PyTorch、CUDA 与 GPU 信息
- 工作流节点及自定义节点版本
- 模型文件名、相对路径、大小和 SHA256
- 输入参数、随机种子和输出路径
- 执行耗时、是否成功、错误信息
- 已知限制与下一步

## 安全约束

- 不提交模型权重、生成缓存、批量输出、虚拟环境或 Python 缓存。
- 不提交令牌、Cookie、SSH 私钥、密码、内网凭据或包含这些信息的日志。
- 不删除现有模型、节点、工作流或输出，除非任务明确要求并得到用户确认。
- 不安装未知自定义节点；安装前检查来源、维护状态、许可证和依赖影响。
- 不执行来源不明工作流中携带的脚本、命令节点或自定义节点。
- 不直接暴露无认证的 ComfyUI 到公网。
- 遇到失败先收集日志、复现条件和环境信息，不进行破坏性修复。

## 模型管理

模型权重只保存在推理主机。Git 中仅记录：

- 文件名和相对路径
- 类型与用途
- 下载来源
- 文件大小
- SHA256
- 许可证或上游模型卡
- 验证状态

无法确认的信息必须标记为 `unverified` 或 `TODO`，不得猜测。

## 完成标准

任务只有在以下条件同时满足时才算完成：

1. 真实 ComfyUI 实例运行成功。
2. 输出文件存在且可读取。
3. 工作流可由干净的命令行调用复现。
4. 报告记录了环境、参数和结果。
5. Pull Request 中没有模型、密钥或无关大文件。
